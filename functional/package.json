{
  "name": "@firebolt-js/mock-firebolt-functional-tests",
  "version": "0.0.1",
  "description": "Command-line interface for controlling the Mock Firebolt server",
  "scripts": {
    "test": "NODE_ENV=test npx jest --config=jest.config.js --silent -i",
    "test:core": "NODE_ENV=test npx jest --config=jest.config.js --forceExit -- ./tests/core.test.mjs",
    "test:healthCheck": "NODE_ENV=test npx jest --config=jest.config.js --forceExit -- ./tests/health-check.test.mjs",
    "test:manage": "NODE_ENV=test npx jest --config=jest.config.js --forceExit -- ./tests/core-manage-jsonrpc.test.mjs",
    "test:discovery": "NODE_ENV=test npx jest --config=jest.config.js --forceExit -- ./tests/core-discovery-jsonrpc.test.mjs",
    "test:core:manage:discovery": "NODE_ENV=test npx jest --config=jest.config.js --forceExit -- ./tests/core-manage-discovery-jsonrpc.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rdkcentral/mock-firebolt.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/rdkcentral/mock-firebolt/issues"
  },
  "homepage": "https://github.com/rdkcentral/mock-firebolt#readme",
  "devDependencies": {
    "@babel/core": "^7.14.6",
    "@babel/node": "^7.14.5",
    "@babel/preset-env": "^7.14.5",
    "jest": "^27.0.4"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "kill-port": "^2.0.1"
  },
  "keywords": [
    "cli",
    "firebolt",
    "mock",
    "os"
  ],
  "license": "Apache-2.0"
}
