<h1>User Successfully Added</h1>

<p>User <strong>{{ userId }}</strong> has been added.</p>

<p>Notes:</p>

<ul>
  <li>When running your Firebolt app, append the following to the URL when launching:</li>
    <ul>
      <li><code>&amp;mf=ws://localhost:9998/{{ userId }}</code></li>
      <li>Note the path at the end of the _apiTarget value!</li>
    </ul>
  <li>When making REST calls, use HTTP header:</li>
    <ul><li><code>X-MockFirebolt-UserId: {{ userId }}</code></li></ul>
  <li>When using the CLI, edit your .mf.config.json file so it looks like this:
    <ul><li><code>userId = {{ userId }}</code></li></ul>
  <li>When using the web admin utility, visit this URL:
    <ul><li><code>https://localhost?userId={{ userId }}</code></li></ul>
</ul>
